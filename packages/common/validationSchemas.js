const authSchema = require('./authSchema');
const projectSchema = require('./projectSchema');
const timeRecordSchema = require('./timeRecordSchema');

module.exports = {
  authSchema,
  projectSchema,
  timeRecordSchema,
};
